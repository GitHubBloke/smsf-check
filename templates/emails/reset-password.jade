extends layouts/default

block body-contents
  - var link = host + '/reset-password/' + user.resetPasswordKey;

  include ./includes/greeting

  p.text-larger
    | You recently requested a link to reset your#{' '}
    strong= name
    |  password.

  p.text-larger Please set a new password by following the link below:

  a.text-larger(href=link)= link

block footer-contents
  include ./includes/regular-footer
